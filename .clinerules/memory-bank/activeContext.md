# ShellGuard TOTP — Active Context

0. [2026-09-05] BUILD 13 RELEASED + FAB REGRESSION UNDER DIAGNOSIS (Cline): Lucas confirmed light-mode text READABLE after Build 12 (theme-aware typography). NEW ISSUE: dashboard speed-dial main FAB renders WHITE in light mode (expected Reef Pink); Lucas flagged "something is happening when you are making edits" — CAREFUL-EDIT MODE requested: no code changes without visual diagnosis. NOTE: screencaps are BLACK on release builds (FLAG_SECURE, production-only — verified via `dumpsys window` SECURE flag + `dumpsys package` no DEBUGGABLE flag). Device = disposable dev Pixel (sailfish/LineageOS, no credentials, no Google services — FOSS/F-Droid target, Play-compatible) — Lucas authorized free ADB wipe/reinstall. IN FLIGHT: uninstall release → install debug Build 13 (app-debug.apk includes typography + scrim fixes, built pre-versionCode-bump) → screencap-driven walkthrough (hatch vault via ADB taps, open speed dial, light mode) → diagnose FAB. Hypothesis logged: code shows light primary = accent.primaryColor (pink) and FAB binds colorScheme.primary — white FAB contradicts wiring; suspect either MONOCHROME accent active (light mono primary = slate 0xFF0F172A — not white either), a stale-build mix, or first-time-seen light-mode issue predating Build 12 (light mode was unreadable before, so FAB appearance in light was never previously evaluated).

0. [2026-09-05] SESSION ARC — v0.0.2.1 SHIPPED THROUGH 3 BUILD CYCLES (Cline): (1) Build 11: Task 22b verify-only / 22c Server&Sync+theme / 22d Import&Export; gateway back-button unclip (e469a1e); closeout commit 25560c6 recovered silently-untracked SettingsImportExportScreen.kt + nav-graph fix; release prep cdcce35 (versionCode 11, 5-anchor sync, RELEASE-v0.0.2.1.md); pipeline green 4m22s, signed .aab/.apk on GitHub Release; root cleanup removed RELEASE-v0.0.1.3.md + RELEASE-v0.0.2.0.md (bodies persist on GitHub Releases; mirror job skips deleted docs; ROADMAP pointer repointed; race-fix 3484218). (2) Build 12 (6ba6d6e, retag): Lucas found ALL light-mode text white/unreadable → root cause `Type.kt` static ShellGuardTypography baked dark tokens (TextPearl≈white, TextMuted, ClawCyan) into every style; fixed via `shellGuardTypography(colors: ShellGuardCustomColors)` factory (Theme.kt:156); + accent swatch border 30% white→outline 60%. Lucas confirmed readable on device. (3) Build 13 (a259e12, retag): speed-dial scrim confined to Scaffold FAB slot (wrap-content, bottom-end anchored) → white edge gaps in light mode; extracted `SpeedDialScrim` full-screen overlay above content/below FAB, speedDialState hoisted, content padding moved off scrim parent Box. Pipeline green each cycle (runs 34007175339 / 34009131063 / 34010289688). versionCode now 13, versionName 0.0.2.1 (SOP durable-conflict pattern); ROADMAP Phase 12→Build 13, Ph13→14, Ph14→15, Ph15→16. PLAY UPLOAD STILL PENDING — wait for FAB diagnosis resolution.

0. [2026-09-05] EPISTEMIC LESSONS THIS SESSION (Cline): (a) Parallel editor+git-commit calls race — the commit won, the ROADMAP edit sat uncommitted; always re-run git status after batched edit+commit. (b) python str.replace without assert silently no-ops (bit twice: nav route, untracked screen file) — assert anchors before relying on a replacement. (c) Diagnose BUILD TYPE before UI debugging: black screencaps mid-session = release build on device (FLAG_SECURE by design), check `dumpsys package <pkg> | grep -E 'versionName|pkgFlags'` + `dumpsys window <pkg> | grep -i secure`. (d) No local release signing (keystore in GH secrets only) → release-build visual fixes must ride the pipeline; retag pre-Play-upload is safe; pipeline-signed APK installs -r over installed release preserving vault. (e) Device under test is disposable (pure dev LineageOS, FOSS target) — wipe/debug-swap authorized for diagnosis. (f) Single Robolectric timing-poll test failure = flake candidate (IntakeOnboardingTest passed clean on isolated re-run); verify before diagnosing. (g) Version hygiene: v0.0.1.1 legitimately skipped (build-numbering policy correction); remaining v0.0.2.0/v0.0.1.3 strings in docs are historical, not anchors.

0. [2026-09-05] v0.0.2.1 (Build 11) RELEASE PREPPED (Cline): Branch feat/phase-11.5-settings-continuity, commits eeb28ea (22c) → eba1855 (22d) → e469a1e (gateway back-button unclip) → 25560c6 (closeout: committed untracked SettingsImportExportScreen.kt, nav-graph fix, ROADMAP 22c/22d checkboxes) → cdcce35 (release prep: versionCode 11 / versionName 0.0.2.1, 5-anchor sync, RELEASE-v0.0.2.1.md, RELEASE-PLAY notes, CHANGELOG). Pre-flight: 101/101 tests (1 Robolectric timing flake in IntakeOnboardingTest re-run clean), 16KB invariants pass (sqlcipher 4.6.1, useLegacyPackaging=false, targetSdk 36). On-device walkthrough verified: hub nav, import/export, theme persistence, tour cutout, gateway back-button. NEXT: push branch + tag v0.0.2.1 → Release Pipeline → Lucas uploads .aab to Play Console internal testing; then Phase 12 (v0.0.2.2).

0. [2026-09-05] Phase 11.5 IMPLEMENTED & VERIFIED (Cline): Branch feat/phase-11.5-settings-continuity, commit eeb28ea. Task 22b complete-by-verification (existing suite covers theme streams). Task 22c built: Appearance Theme section (Abyssal/Contrast/System + 6 accent swatches, legacy tags preserved), tiles extracted to SettingsControls.kt, new SettingsServerSyncScreen (Sync Now via TotpViewModel.refreshRemoteVault, Connect-to-Gateway, Disconnect w/ dialog), Spotlight Tour step 2 migrated, AuthViewModel gained currentSession/logout/tourStep/setGuidedTourCompleted, hub gained Server & Sync category, placeholder copy fixed, legacy SettingsScreen.kt + route deleted. VERIFIED: 101/101 tests green, assembleDebug OK (67MB APK). PENDING: on-device ADB verification (Pixel 8: theme live-preview, Gateway roundtrip, tour step 2), then release prep (versionCode 11 = v0.0.2.1, 5-anchor sync, tag, pipeline).

0. [2026-09-05] Phase 11.5 Corpus Weave COMPLETE (Cline): v0.0.1.3 settings parity audit done; new **Phase 11.5: Settings Continuity [v0.0.2.1 (Build 11)]** woven into ROADMAP.md (v2.5.0) + meta-prompt Stage 12.5. Task 22b (theme mode + ThemeAccent StateStreams — verify Task 10's existing pref pathway, don't re-derive) and Task 22c (Appearance "Theme" section + new `SettingsServerSyncScreen` hub category restoring Gateway login/Sync Now/Disconnect; fix placeholder copy; retire legacy SettingsScreen route + Local Storage card). Phase 12 renumbered → v0.0.2.2 (Build 12); Phases 13–15 Builds bumped +1 (Build 13/14/15). Structural contract added: ui-ux-design-system.md §4.A.5 hub route map + invariants; DESIGN.md §9 architecture note. RELEASE-v0.0.2.0.md next-stop pointer updated. NO code written yet — next session: branch `feat/phase-11.5-settings-continuity`, implement 22b then 22c, gradle background/poll pattern (~13min), ADB verify on Pixel 8.

0. [2026-09-04] v0.0.1.3 Hotfix RELEASED & CI-Verified (Cline): Commit 3e70fa9 + tag v0.0.1.3 pushed to origin; Release Pipeline passed 3m46s publishing signed .aab (35.2MB) + .apk (59.6MB) to GitHub Release. Version sync across all anchors (versionCode 9, CHANGELOG, RELEASE-v0.0.1.3.md, RELEASE-PLAY.md, README badge). Next session focus: Play Console tester upload if Lucas wants, then Phase 11/12 work.
-1. [2026-09-05] Phase 11 SHIPPED — v0.0.2.0 (Build 10) Milestone 2 Released (Cline): Tasks 21+22 implemented (preferences stores + Settings Hub) and verified live on Lucas's new test device — original Pixel (sailfish, LineageOS) via wireless ADB; Settings Hub + Appearance/Behavior screens captured into store-assets and README screenshots refreshed (slot 5 → hub, new row 3). Play workflow doc invariants refreshed (101+ tests, Cline memory-bank paths). Release executed via version-update Step 5/6: 5-anchor sync (versionCode 10, versionName 0.0.2.0), tag v0.0.2.0 pushed, Release Pipeline green 4m17s (run 33981002398), signed .aab (35.2MB) + .apk published to GitHub Release. Next: Lucas uploads .aab to Play Console internal testing (workflow Step 6/7); then Phase 12 Security Suite (Tasks 23/24, v0.0.2.1 Build 11) — includes the spec'd FLAG_SECURE toggle + Audit Log DAO.
-1. [2026-09-04] Memory Bank Full Sync (Cline): Refreshed all core files post-v0.0.1.3 — progress.md updated from stale "v0.0.1.2 in progress" to released state (v0.0.1.3 hotfix shipped, 96/96 tests, Task 24 spec expansion, /learn codifications); techContext.md gained Device & CI Tooling section (adb path, gh CLI, Release Pipeline trigger behavior); changelog [session-3] added. Known-issues watch item recorded: server-side pearls `updated_at` is null/missing — delta optimization dormant until ShellGuard Server populates it.
1. [2026-09-04] CRITICAL SYNC REGRESSION FIXED — v0.0.1.2 remote sync silently dropped all pearls (Cline): Lucas reported v0.0.1.2 no longer syncing remote codes while v0.0.1.0 worked. Root cause: `TotpRepository.syncRemoteVault` delta filter compared `existingRemoteByUpdatedAt[id] == pearl.updated_at` with BOTH nullable — when server `updated_at` is null/missing (PearlDto.updated_at is `String? = null`), null==null classified every pearl as "unchanged" → never decrypted, never upserted; prune-only reconciliation can't insert → zero codes land while sync returns Result.success (silent, no logs). Fix: extracted `internal companion fun classifyDeltaPearls(localSnapshotById, pearls)` in TotpRepository — unchanged ONLY when local row exists AND pearl.updated_at != null AND stamps equal; null stamps always sync (self-healing for stuck devices). Added `DeltaSyncClassificationTest` (6 cases incl. exact repro). Verified 96/96 tests green + assembleDebug (67MB APK). Test run note: gradle runs take ~13min in container and get cut at 300s tool timeout — use background/poll pattern, and beware stale XML in app/build/test-results (verify per-suite XML presence, not just totals). On-device sync verification pending Lucas.
1. [2026-09-04] UI Fix: TotpCard Read-Only Badge Wrap + Task 24 FLAG_SECURE Toggle Spec Expanded (Cline): Lucas reported the 'Read-only' pill wrapping ('Rea/d-on/ly') and inflating card height. Verified live via ADB screencap on Pixel 8. First fix (maxLines=1+softWrap=false) clipped to 'Reac'; per Lucas's choice the pill text was removed in favor of the cloud icon alone (contentDescription 'Synced from Server (Read-Only)') — card now renders single-line, verified on-device. Secondary discovery: black 15KB screencaps were display-suspend artifacts, NOT FLAG_SECURE (debug builds exempt via !BuildConfig.DEBUG gate in MainActivity.kt:53). Expanded Task 24 'Screen security toggle' in ROADMAP.md + project/meta-prompt-ai-studio.md into full spec: allowScreenshots DataStore pref (default false), opt-in confirm dialog, StateFlow-driven immediate window flag apply, forced FLAG_SECURE when vault locked/backgrounded, debug exemption preserved, black-screencap QA note, SCREEN_SECURITY_CHANGED audit event, Task 23 controller dependency note. Uncommitted work on main: sync regression fix (classifyDeltaPearls + DeltaSyncClassificationTest, 96/96 green), TotpCard tweak, Task 24 spec expansion — v0.0.1.3 release prep pending Lucas's go.
## Sliding Window of Events
1. [2026-09-03] Release Review, versionCode Correction & Sync Efficiencies (Cline): Reviewed release-agent changes; approved One-Way Sync hardening. Corrected versionCode 9→8 (Build 8) in build.gradle.kts + RELEASE-v0.0.1.2.md, RELEASE-PLAY.md, CHANGELOG.md, ROADMAP.md Phase 10 heading. Implemented two approved efficiencies: (a) TotpViewModel.deleteItem(id) now uses conditional SQL delete `deleteByIdIfLocal` in TotpItemDao (is_local_only = 1 OR owner_uuid = 'local') — One-Way Sync guard enforced at the SQL layer with zero pre-fetch reads; (b) TotpRepository.syncRemoteVault client-side delta — pearls whose remote `updated_at` matches the local mirror (via new `getRemoteItemsOnce` snapshot query) skip AES-GCM decryption AND re-upsert; pruning spans unchanged+changed ids so mirrors survive while server-deleted rows are still pruned. Verified 90/90 tests green + assembleDebug. Disk: root was the 30G Unraid docker.img loop at 100%; Lucas converted to directory mode → root now 3.7T array (106G free); /tmp purge cancelled per user scope.
2. [2026-09-02] One-Way Sync Hardening & Pull-to-Refresh Dashboard Alignment: Verified and codified strict one-way mirror sync invariants. Updated TotpSyncWorker to 6-hour periodic background schedule (ExistingPeriodicWorkPolicy.UPDATE). Implemented Compose Material 3 PullToRefreshBox on TotpListScreen backed by refreshRemoteVault in TotpViewModel. Completely locked remote synced codes to read-only on device (disabled edit modal, disabled delete confirmation, disabled swipe-to-dismiss, added visual 'Read-only' indicator to TotpCard). Fixed misleading AddSecretScreen banner copy from 'Token will sync to connected server' to local storage banner. Changed IntakeViewModel import syncState from PENDING_SYNC to LOCAL. Authored OneWaySyncAndReadOnlyProtectionTest verifying all protections (90/90 tests passing 100% green). Streamed and verified updated APK live on physical Pixel 8.
2. [2026-09-02] Phase 10 Speed Dial Implemented & Release v0.0.1.2 (Build 9) Prepped: Implemented ExpandableSpeedDialFab with 45-degree morph (+ to ✕), dark scrim, and 3 staggered pills (Scan QR / Scan image / Enter manually). Extracted unified ImageQrDecoder with Google ML Kit for gallery QR screenshot decoding. Refactored QrScannerScreen onto shared decoder and removed dead ScannerFab.kt. Updated test oracle in LocalModeUnlockAndVaultTest. Verified 86/86 unit tests green and assembleDebug successful. Prepped RELEASE-v0.0.1.2.md, updated RELEASE-PLAY.md, bumped versionCode = 9 & versionName = "0.0.1.2", and aligned ROADMAP.md.

## 🤝 Next Agent Handoff
**Current State**:
- Working tree on branch `feat/phase-10-speed-dial`.
- Phase 10 (Tasks 19 & 20) fully implemented and verified.
- All 90 local unit tests are passing 100% green (`./gradlew testDebugUnitTest`).
- Release `v0.0.1.2 (Build 8)` prepped: `RELEASE-v0.0.1.2.md` authored, `RELEASE-PLAY.md` prepended, `app/build.gradle.kts` bumped to `versionCode = 8` & `versionName = "0.0.1.2"` (corrected from 9 per versioning policy), `ROADMAP.md` checked off.
- Sync efficiencies shipped: SQL-layer delete guard + client-side delta decryption skip.

**Immediate Directive for Next Agent**:
Following merge and release of `v0.0.1.2 (Build 8)`, begin implementation of **Phase 11: Categorized Settings Hub & Appearance/Behavior Customization [v0.0.2.0 (Build 10) — Milestone 2]**:
1. **Branching**: Start on a fresh branch off `main`: `git checkout -b feat/phase-11-settings-hub`.
2. **Roadmap Reference**: Open [`ROADMAP.md`](ROADMAP.md#phase-11-categorized-settings-hub--appearancebehavior-customization-v0020-build-10--milestone-2) and review **Task 21** and **Task 22**.
3. **Execution Prompt**: Read Stage 12 in [`project/meta-prompt-ai-studio.md`](project/meta-prompt-ai-studio.md#stage-12-phase-11-prompt--categorized-settings-hub--appearancebehavior-customization-v0020-build-10--milestone-2).
4. **Task Breakdown**:
   - **Task 21: [Functionality] Preferences Store Architecture & Entry Formatting Engine**:
     - Expand `AuthRepository` and preferences store (`AppearancePreferences`, `BehaviorPreferences`).
   - **Task 22: [UI Component] Categorized Settings Hub (`SettingsMetaScreen`), Appearance & Behavior Sub-screens**:
     - Implement `SettingsMetaScreen.kt`, `SettingsAppearanceScreen.kt`, and `SettingsBehaviorScreen.kt`.
5. **Local Verification**: Run `./gradlew testDebugUnitTest` using:
   ```bash
   export JAVA_HOME="/config/Applications/android-studio/jbr"
   export PATH="$JAVA_HOME/bin:$PATH"
   export GRADLE_OPTS="-XX:-UsePerfData -Djava.io.tmpdir=$PWD/app/build/tmp"
   ./gradlew testDebugUnitTest --no-daemon
   ```
