# ShellGuard TOTP — Active Context

0. [2026-09-04] v0.0.1.3 Hotfix RELEASED & CI-Verified (Cline): Commit 3e70fa9 + tag v0.0.1.3 pushed to origin; Release Pipeline passed 3m46s publishing signed .aab (35.2MB) + .apk (59.6MB) to GitHub Release. Version sync across all anchors (versionCode 9, CHANGELOG, RELEASE-v0.0.1.3.md, RELEASE-PLAY.md, README badge). Next session focus: Play Console tester upload if Lucas wants, then Phase 11/12 work.
1. [2026-09-04] CRITICAL SYNC REGRESSION FIXED — v0.0.1.2 remote sync silently dropped all pearls (Cline): Lucas reported v0.0.1.2 no longer syncing remote codes while v0.0.1.0 worked. Root cause: `TotpRepository.syncRemoteVault` delta filter compared `existingRemoteByUpdatedAt[id] == pearl.updated_at` with BOTH nullable — when server `updated_at` is null/missing (PearlDto.updated_at is `String? = null`), null==null classified every pearl as "unchanged" → never decrypted, never upserted; prune-only reconciliation can't insert → zero codes land while sync returns Result.success (silent, no logs). Fix: extracted `internal companion fun classifyDeltaPearls(localSnapshotById, pearls)` in TotpRepository — unchanged ONLY when local row exists AND pearl.updated_at != null AND stamps equal; null stamps always sync (self-healing for stuck devices). Added `DeltaSyncClassificationTest` (6 cases incl. exact repro). Verified 96/96 tests green + assembleDebug (67MB APK). Test run note: gradle runs take ~13min in container and get cut at 300s tool timeout — use background/poll pattern, and beware stale XML in app/build/test-results (verify per-suite XML presence, not just totals). On-device sync verification pending Lucas.
1. [2026-09-04] UI Fix: TotpCard Read-Only Badge Wrap + Task 24 FLAG_SECURE Toggle Spec Expanded (Cline): Lucas reported the 'Read-only' pill wrapping ('Rea/d-on/ly') and inflating card height. Verified live via ADB screencap on Pixel 8. First fix (maxLines=1+softWrap=false) clipped to 'Reac'; per Lucas's choice the pill text was removed in favor of the cloud icon alone (contentDescription 'Synced from Server (Read-Only)') — card now renders single-line, verified on-device. Secondary discovery: black 15KB screencaps were display-suspend artifacts, NOT FLAG_SECURE (debug builds exempt via !BuildConfig.DEBUG gate in MainActivity.kt:53). Expanded Task 24 'Screen security toggle' in ROADMAP.md + project/meta-prompt-ai-studio.md into full spec: allowScreenshots DataStore pref (default false), opt-in confirm dialog, StateFlow-driven immediate window flag apply, forced FLAG_SECURE when vault locked/backgrounded, debug exemption preserved, black-screencap QA note, SCREEN_SECURITY_CHANGED audit event, Task 23 controller dependency note. Uncommitted work on main: sync regression fix (classifyDeltaPearls + DeltaSyncClassificationTest, 96/96 green), TotpCard tweak, Task 24 spec expansion — v0.0.1.3 release prep pending Lucas's go.
## Sliding Window of Events
1. [2026-09-03] Release Review, versionCode Correction & Sync Efficiencies (Cline): Reviewed release-agent changes; approved One-Way Sync hardening. Corrected versionCode 9→8 (Build 8) in build.gradle.kts + RELEASE-v0.0.1.2.md, RELEASE-PLAY.md, CHANGELOG.md, ROADMAP.md Phase 10 heading. Implemented two approved efficiencies: (a) TotpViewModel.deleteItem(id) now uses conditional SQL delete `deleteByIdIfLocal` in TotpItemDao (is_local_only = 1 OR owner_uuid = 'local') — One-Way Sync guard enforced at the SQL layer with zero pre-fetch reads; (b) TotpRepository.syncRemoteVault client-side delta — pearls whose remote `updated_at` matches the local mirror (via new `getRemoteItemsOnce` snapshot query) skip AES-GCM decryption AND re-upsert; pruning spans unchanged+changed ids so mirrors survive while server-deleted rows are still pruned. Verified 90/90 tests green + assembleDebug. Disk: root was the 30G Unraid docker.img loop at 100%; Lucas converted to directory mode → root now 3.7T array (106G free); /tmp purge cancelled per user scope.
2. [2026-09-02] One-Way Sync Hardening & Pull-to-Refresh Dashboard Alignment: Verified and codified strict one-way mirror sync invariants. Updated TotpSyncWorker to 6-hour periodic background schedule (ExistingPeriodicWorkPolicy.UPDATE). Implemented Compose Material 3 PullToRefreshBox on TotpListScreen backed by refreshRemoteVault in TotpViewModel. Completely locked remote synced codes to read-only on device (disabled edit modal, disabled delete confirmation, disabled swipe-to-dismiss, added visual 'Read-only' indicator to TotpCard). Fixed misleading AddSecretScreen banner copy from 'Token will sync to connected server' to local storage banner. Changed IntakeViewModel import syncState from PENDING_SYNC to LOCAL. Authored OneWaySyncAndReadOnlyProtectionTest verifying all protections (90/90 tests passing 100% green). Streamed and verified updated APK live on physical Pixel 8.
2. [2026-09-02] Phase 10 Speed Dial Implemented & Release v0.0.1.2 (Build 9) Prepped: Implemented ExpandableSpeedDialFab with 45-degree morph (+ to ✕), dark scrim, and 3 staggered pills (Scan QR / Scan image / Enter manually). Extracted unified ImageQrDecoder with Google ML Kit for gallery QR screenshot decoding. Refactored QrScannerScreen onto shared decoder and removed dead ScannerFab.kt. Updated test oracle in LocalModeUnlockAndVaultTest. Verified 86/86 unit tests green and assembleDebug successful. Prepped RELEASE-v0.0.1.2.md, updated RELEASE-PLAY.md, bumped versionCode = 9 & versionName = "0.0.1.2", and aligned ROADMAP.md.
3. [2026-09-02] Release v0.0.1.0 Cloud Build Passed & Cleaned: Cloud CI on tag `v0.0.1.0` completed successfully, producing signed `.aab` and `.apk` release artifacts. Deleted obsolete `RELEASE-v0.0.0.2.md`. Codified headless KeyStore and Robolectric SQLite testing invariants in `AGENTS.md` and `android-keystore-cold-restart-testing` skill. Aligned `project/meta-prompt-ai-studio.md` Stage 11 numbering directly with `ROADMAP.md` Phase 10. Local test suite verified 100% green across all 79 unit tests.
4. [2026-09-02] Release v0.0.1.0 (Build 7) Retagged & Pushed: Diagnosed headless CI test failures (AndroidKeyStore NoSuchAlgorithmException and SQLiteConnectionNatives UnsatisfiedLinkError). Added HMAC fallback in AndroidKeyStoreHelper, explicit FrameworkSQLiteOpenHelperFactory in Robolectric tests, -XX:-UsePerfData in gradle.properties, and redirected test java.io.tmpdir to build/tmp. Re-ran full test suite locally (79/79 passing 100% green). Committed under ff344a3, force-updated tag v0.0.1.0, and pushed to origin to trigger passing GitHub Actions release build.
5. [2026-09-02] Release v0.0.1.0 (Build 7) Tagged & Pushed to GitHub: Completed Phase 9 (Vault Security Education, Enlarged Spotlight Tour & Empty Vault Landing) + One-Way Mirror Sync grouped architecture. Fixed two test regressions in `LocalModeUnlockAndVaultTest` and `BackupManagerTest` prior to push. Created `RELEASE-v0.0.1.0.md`, updated `RELEASE-PLAY.md` with grouped separation highlights, bumped `versionCode = 7` & `versionName = 0.0.1.0`, merged to `main`, and pushed annotated tag `v0.0.1.0`.
6. [2026-09-02] Phase 15 Planned & Written to Docs: Designed ClawKey (`hu-`) vault creation mode as Phase 15 (Tasks 29 & 30). Appended full spec to ROADMAP.md and Stage 16 execution prompt to meta-prompt-ai-studio.md. Key design decisions: `ClawKeyInputForm` dual-tab (Paste / Upload .json), `ClawKeyValidator` as single source of truth (`startsWith("hu-") && length == 67`), CLAWKEY routes through existing password KDF pathway, pre-import duplicate resolution by normalized secret+title fingerprint, export stamping via `vaultMode.name`, LockScreen CLAWKEY branch.

## 🤝 Next Agent Handoff
**Current State**:
- Working tree on branch `feat/phase-10-speed-dial`.
- Phase 10 (Tasks 19 & 20) fully implemented and verified.
- All 90 local unit tests are passing 100% green (`./gradlew testDebugUnitTest`).
- Release `v0.0.1.2 (Build 8)` prepped: `RELEASE-v0.0.1.2.md` authored, `RELEASE-PLAY.md` prepended, `app/build.gradle.kts` bumped to `versionCode = 8` & `versionName = "0.0.1.2"` (corrected from 9 per versioning policy), `ROADMAP.md` checked off.
- Sync efficiencies shipped: SQL-layer delete guard + client-side delta decryption skip.

**Immediate Directive for Next Agent**:
Following merge and release of `v0.0.1.2 (Build 8)`, begin implementation of **Phase 11: Categorized Settings Hub & Appearance/Behavior Customization [v0.0.2.0 (Build 10) — Milestone 2]**:
1. **Branching**: Start on a fresh branch off `main`: `git checkout -b feat/phase-11-settings-hub`.
2. **Roadmap Reference**: Open [`ROADMAP.md`](ROADMAP.md#phase-11-categorized-settings-hub--appearancebehavior-customization-v0020-build-10--milestone-2) and review **Task 21** and **Task 22**.
3. **Execution Prompt**: Read Stage 12 in [`project/meta-prompt-ai-studio.md`](project/meta-prompt-ai-studio.md#stage-12-phase-11-prompt--categorized-settings-hub--appearancebehavior-customization-v0020-build-10--milestone-2).
4. **Task Breakdown**:
   - **Task 21: [Functionality] Preferences Store Architecture & Entry Formatting Engine**:
     - Expand `AuthRepository` and preferences store (`AppearancePreferences`, `BehaviorPreferences`).
   - **Task 22: [UI Component] Categorized Settings Hub (`SettingsMetaScreen`), Appearance & Behavior Sub-screens**:
     - Implement `SettingsMetaScreen.kt`, `SettingsAppearanceScreen.kt`, and `SettingsBehaviorScreen.kt`.
5. **Local Verification**: Run `./gradlew testDebugUnitTest` using:
   ```bash
   export JAVA_HOME="/config/Applications/android-studio/jbr"
   export PATH="$JAVA_HOME/bin:$PATH"
   export GRADLE_OPTS="-XX:-UsePerfData -Djava.io.tmpdir=$PWD/app/build/tmp"
   ./gradlew testDebugUnitTest --no-daemon
   ```
